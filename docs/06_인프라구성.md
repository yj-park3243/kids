# 인프라 구성

> match 프로젝트와 동일한 AWS 계정/네이버 지도 키 사용. EC2, S3, RDS는 kids 전용으로 신규 생성.

---

## 1. 공유 리소스 (match 프로젝트와 동일)

| 항목 | 값 | 비고 |
|------|-----|------|
| AWS 계정 | match와 동일 계정 | 동일 IAM 사용 |
| AWS Region | ap-northeast-2 (서울) | |
| AWS Access Key ID | `AKIAXRJAMOGYMIM3YVN7` | match와 동일 |
| Naver Map Client ID | `539desbv96` | match와 동일 |
| Firebase 프로젝트 | 신규 생성 필요 (kids 전용) | match는 `pins-488a8` 사용 중 |

---

## 2. 신규 생성 리소스 (kids 전용)

### 2.1 EC2 (생성 완료)

| 항목 | 값 |
|------|-----|
| 인스턴스 ID | `i-0b3b780aa1b72a9c1` |
| 인스턴스 이름 | kids-server |
| AMI | Ubuntu 22.04 ARM64 (`ami-08e312f3e097884eb`) |
| 인스턴스 타입 | t4g.small |
| 키페어 | `kids-key.pem` (로컬: `/Users/yongju/WebProject2/kids/kids-key.pem`) |
| 보안그룹 | `kids-sg` (`sg-0a65c788236bd219c`) |
| 인바운드 규칙 | 22(SSH), 80(HTTP), 443(HTTPS), 3000(NestJS) |
| Elastic IP | `43.201.221.240` (`eipalloc-03f99a2c2d5c3a317`) |
| SSH 접속 | `ssh -i kids-key.pem ubuntu@43.201.221.240` |
| 설치 완료 | Node.js 20, Nginx, Redis, PM2, Git |

### 2.2 RDS PostgreSQL (생성 완료)

| 항목 | 값 |
|------|-----|
| 인스턴스 식별자 | `kids-db` |
| 엔진 | PostgreSQL 16.13 |
| 인스턴스 타입 | db.t3.micro |
| DB 이름 | kids |
| 마스터 사용자 | `kids_admin` |
| 마스터 비밀번호 | `KidsDb2026!` |
| 엔드포인트 | `kids-db.cuhooenm6qww.ap-northeast-2.rds.amazonaws.com:5432` |
| 스토리지 | 20GB gp3 |
| 퍼블릭 액세스 | 아니요 (EC2에서만 접근) |
| 보안그룹 | `kids-db-sg` (`sg-02c2279d7ab945df0`) |

### 2.3 S3 (생성 완료)

| 항목 | 값 |
|------|-----|
| 버킷 이름 | `kids-uploads-518` |
| 리전 | ap-northeast-2 |
| 용도 | **프로필 이미지, 채팅 이미지 저장 (Firebase Storage 대신 S3 사용)** |
| 퍼블릭 액세스 | 차단 (presigned URL 사용) |
| CORS | 설정 완료 |
| 서버 SDK | `@aws-sdk/client-s3` (`server/src/upload` 모듈) |

### 2.4 Firebase (신규 프로젝트)

| 항목 | 값 |
|------|-----|
| 프로젝트 ID | `kids-app-518` |
| Firestore | **사용 안 함** — 채팅은 PostgreSQL + NestJS WebSocket Gateway (Socket.IO `/chat` namespace) |
| Firebase Storage | **사용 안 함** — 이미지는 S3(`kids-uploads-518`) 사용 |
| FCM | 푸시 알림 (서버에서 Firebase Admin SDK `getMessaging()`) + APNs key |
| Auth | Apple/Google OAuth Client ID 자동 생성용으로 활용 (iOS/Android/Web) |
| Android 앱 | `kr.kids.app` — `google-services.json` 배치 완료 |
| iOS 앱 | `kr.kids.app` — `GoogleService-Info.plist` 배치 완료 |
| 위치 | asia-northeast3 (서울) |

---

## 3. 도메인 / 네트워크 (완료)

| 항목 | 값 | 비고 |
|------|-----|------|
| 도메인 | `growtogether.kr` | 가비아 구매, AWS Route 53로 위임 |
| Hosted Zone | `Z046893625HIJQKO26O7M` | Route 53 |
| API 서브도메인 | `api.growtogether.kr` → A 레코드 `43.201.221.240` | NestJS 서버 |
| 어드민 서브도메인 | `admin.growtogether.kr` → `43.201.221.240` | React SPA 정적 호스팅 (Nginx) |
| SSL | Let's Encrypt (Certbot) 자동갱신 | api/admin 양쪽 인증서 발급 완료 |
| 리버스 프록시 | Nginx — 80 → 443 redirect, 443 → `localhost:3000` (NestJS) | EC2에 설치 |

---

## 4. 서버 환경변수 (.env.production)

```env
# App
NODE_ENV=production
PORT=3000

# Database (RDS)
DB_HOST=kids-db.cuhooenm6qww.ap-northeast-2.rds.amazonaws.com
DB_PORT=5432
DB_NAME=kids
DB_USER=kids_admin
DB_PASSWORD=<RDS 생성 시 설정한 비밀번호>

# JWT
JWT_SECRET=<32자 이상 랜덤 문자열>
JWT_ACCESS_EXPIRES=1h
JWT_REFRESH_EXPIRES=14d

# AWS (match와 동일 계정)
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=AKIAXRJAMOGYMIM3YVN7
AWS_SECRET_ACCESS_KEY=<match 서버 .env 참고>
AWS_S3_BUCKET=<kids 전용 S3 버킷명>

# Kakao (서버 구현 완료, 앱 SDK 미연결)
KAKAO_REST_API_KEY=<카카오 개발자 콘솔에서 새 앱 등록>

# Apple Sign In (Team 62YWN6BZ3U, Key N48C3RP57P)
APPLE_CLIENT_ID=kr.kids.app                # 또는 Services ID
APPLE_TEAM_ID=62YWN6BZ3U
APPLE_KEY_ID=N48C3RP57P
APPLE_PRIVATE_KEY=<AuthKey_N48C3RP57P.p8 파일 내용>

# Google (Firebase Auth가 자동 생성한 OAuth client — iOS/Android/Web)
GOOGLE_CLIENT_ID=<Firebase Console > Authentication > Google에서 확인>

# Firebase Admin SDK (FCM 전용)
FIREBASE_PROJECT_ID=kids-app-518
FIREBASE_CLIENT_EMAIL=<서비스 계정 이메일>
FIREBASE_PRIVATE_KEY=<서비스 계정 프라이빗 키>
# FIREBASE_STORAGE_BUCKET — 미사용 (이미지는 S3)

# KCP V2 본인인증 (match와 동일 키)
KCP_SITE_CD=ALQ1Q
KCP_SITE_KEY=<match와 동일>
KCP_RETURN_URL=https://api.growtogether.kr/v1/auth/kcp/callback

# Naver Map (match와 동일 키)
NAVER_MAP_CLIENT_ID=539desbv96
NAVER_MAP_CLIENT_SECRET=<match 네이버 개발자 콘솔에서 확인>

# Redis (설치 완료, 현재 서버 코드에서는 미사용 — 향후 세션/캐시 도입 시 활성화)
# REDIS_HOST=localhost
# REDIS_PORT=6379
```

---

## 5. Flutter 앱 설정

### Naver Map 설정

**Android** (`android/app/src/main/AndroidManifest.xml`)
```xml
<meta-data android:name="com.naver.maps.map.CLIENT_ID" android:value="539desbv96"/>
```

**iOS** (`ios/Runner/Info.plist`)
```xml
<key>NMFClientId</key>
<string>539desbv96</string>
```

### Firebase 설정

- `google-services.json` (Android) → kids Firebase 프로젝트에서 다운로드
- `GoogleService-Info.plist` (iOS) → kids Firebase 프로젝트에서 다운로드
- `firebase_options.dart` → `flutterfire configure` 명령으로 자동 생성

---

## 6. 인프라 생성 현황

```
[완료] AWS 리소스 생성
   ├── [완료] EC2 키페어 (kids-key.pem)
   ├── [완료] 보안그룹 (kids-sg, kids-db-sg)
   ├── [완료] RDS PostgreSQL 16.13
   ├── [완료] S3 버킷 (kids-uploads-518)
   ├── [완료] EC2 인스턴스 (t4g.small, Ubuntu 22.04)
   └── [완료] Elastic IP (43.201.221.240)

[완료] EC2 서버 셋업
   ├── [완료] Node.js 20
   ├── [완료] Nginx + 리버스 프록시 (80→443 redirect, 443 → localhost:3000)
   ├── [완료] Redis (설치됨, 현재 코드 미사용)
   ├── [완료] PM2 (ecosystem.config.cjs)
   └── [완료] SSL 인증서 — Let's Encrypt(Certbot) 자동갱신

[완료] 본인인증
   └── KCP V2 (사이트코드 ALQ1Q) — match와 동일 키 사용

[완료] Firebase 프로젝트 (kids-app-518)
   ├── [완료] Android / iOS 앱 등록
   ├── [완료] google-services.json / GoogleService-Info.plist 배치
   ├── [완료] firebase_options.dart 생성
   ├── [N/A] Firestore — 미사용 (NestJS WebSocket으로 대체)
   ├── [N/A] Firebase Storage — 미사용 (S3 사용)
   ├── [완료] FCM 서비스 계정 키 → server/.env
   └── [완료] APNs key 업로드

[완료] 소셜 로그인 앱 등록
   ├── [완료] Apple Developer — Sign in with Apple (Key N48C3RP57P, Team 62YWN6BZ3U)
   ├── [완료] Google — Firebase Auth 자동 생성 OAuth client (iOS/Android/Web)
   └── [미완] Kakao — 서버 구현 완료, 앱 SDK 미연결

[완료] 도메인 연결
   ├── [완료] growtogether.kr 구매 + Route 53 hosted zone (Z046893625HIJQKO26O7M)
   ├── [완료] api.growtogether.kr → 43.201.221.240
   └── [완료] admin.growtogether.kr → 43.201.221.240 (Nginx 정적 호스팅)
```

---

## 7. 배포 방식

| 항목 | 방식 |
|------|------|
| 서버 배포 | 로컬 `server/deploy.sh` → rsync로 EC2 전송 → 원격에서 `npm install` + `npm run build` → `pm2 restart` (`ecosystem.config.cjs`) |
| 앱 배포 | `app/deploy.sh` (fastlane 래퍼) — `fastlane deploy` (App Store/Play 정식 트랙), `fastlane internal` (TestFlight / Play 내부 트랙) |
| 어드민 배포 | `admin/deploy.sh` — Vite `npm run build` → `admin.growtogether.kr` Nginx 정적 루트로 업로드 |
| DB 마이그레이션 | TypeORM migration:run |
